name: healthz-bench
services:
  bench:
    build:
      context: ../../../..
      dockerfile: sdk-api/examples/100-base/healthz/Dockerfile
    ports:
      - "23100:23100"
    environment:
      DOCKER_TEST: "1"
