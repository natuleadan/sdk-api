name: healthz-bench
services:
  bench:
    build:
      context: ../../..
      dockerfile: sdk-api/examples/100-healthz/Dockerfile
    environment:
      DOCKER_TEST: "1"
      RPS_BENCH: "${RPS_BENCH}"
