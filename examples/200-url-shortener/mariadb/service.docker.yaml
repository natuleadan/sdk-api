name: url-shortener
port: 18088

databases:
  - name: maria-main
    driver: mysql
    url: "${DATABASE_URL}"
    pool:
      max_conns: 100
      min_conns: 50

entry:
  - type: crud
    model: Link
    db: maria-main
    table: link
    path: /links

  - type: crud
    model: LinkExpand
    db: maria-main
    table: link
    path: /expand
    overrides:
      list: "-"
      create: "-"
      update: "-"
      delete: "-"

server:
  host: "0.0.0.0"
  prefork: true
  api_prefix: /api/v1
  max_conns: 10000
  timeout: 30s
  middleware:
    - path: "/api/v1/*"
      apply:
        - logger
