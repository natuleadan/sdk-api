name: url-link-nats
port: 18084

databases:
  - name: pg-main
    driver: postgres
    url: "${DATABASE_URL:-postgres://dev:devpass@pgdog:6432/postgres?sslmode=disable}"
    pool:
      max_conns: 500
      min_conns: 10

event_streams:
  - name: primary
    driver: nats
    url: "nats://nats:4222"

entry:
  - type: crud
    model: Link
    db: pg-main
    table: link
    path: /links
  - type: rest
    method: GET
    path: /expand/:shortCode
    handler: expandLink

server:
  host: "0.0.0.0"
  prefork: true
  api_prefix: /api/v1
  max_conns: 10000
  timeout: 30s
  middleware:
    - path: "/api/v1/*"
      apply:
        - logger
