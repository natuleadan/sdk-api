name: file-upload
services:
  app:
    build:
      context: ../../..
      dockerfile: sdk-api/examples/file-upload/Dockerfile
    ports:
      - "18090:18090"
    volumes:
      - uploads:/data/uploads
volumes:
  uploads:
