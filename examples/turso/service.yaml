name: turso-bench
port: 18086

databases:
  - name: turso-main
    driver: turso
    url: "${TURSO_URL}"

server:
  host: "0.0.0.0"
  api_prefix: /api/v1
  prefork: true
  max_conns: 10000
  timeout: 30s
  middleware:
    - path: "/api/v1/*"
      apply:
        - logger

